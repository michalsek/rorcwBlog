<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>Blog - <%= yield :title %></title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true%>
  <%= javascript_include_tag 'application','data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

  <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
  <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
  <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
  <![endif]-->
</head>
<body>
  <header>
    <div class="container vertical-align" >
      <div class="col-xs-5"><h2>Blog - <%= yield :title %></h2></div>
      <div class="col-xs-7">
        <nav class="page-nav text-right">
          <ul>
            <li><%= link_to @current_user.name, user_path(@current_user) unless @current_user.nil? %></li>
            <li><%= link_to "Główna", root_path %></li>
            <li><%= link_to "Kategorie", categories_path %></li>
            <li><%= link_to "Najnowsze wpisy", recent_posts_path %></li>
            <li><%= link_to "Wydarzenia", events_path %></li>
            <% if @current_user %>
              <li><%= link_to "Wyloguj", logout_path %></li>
            <% else %>
              <li><%= link_to "Zaloguj", login_path %></li>
            <% end %>
          </ul>
        </nav>
      </div>
    </div>
  </header>
	<div class="container">
    <div class="notice">
      <div class="container <%= "visible" unless notice.nil? %>"><%= notice %></div>
    </div>
		<%= yield %>
	</div>
</body>
</html>